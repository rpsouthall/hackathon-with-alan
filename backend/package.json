{
  "name": "worldtalk-voice-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env src/dev.mjs",
    "test": "node --test test/*.test.mjs",
    "dev:live": "node --env-file-if-exists=.env src/live-server.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
